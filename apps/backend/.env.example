# Port the server listens on
PORT=3000

# Root directory for all user data (settings, conversations, memory, etc.)
# Must be an absolute path. Defaults to ~/.omni-craft if not set.
# DATA_DIR=/absolute/path/to/data

# Port for the embedded VSCode web server (code serve-web). Required.
VSCODE_PORT=18927
